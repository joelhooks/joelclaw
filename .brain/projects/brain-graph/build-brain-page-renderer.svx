---
title: "Build the Brain Page Renderer"
type: "question"
kind: "task"
status: "closed"
interaction: "AFK"
parent: "./brain-graph-brief.svx"
blockers: []
created_at: "2026-07-12"
closed_at: "2026-07-12"
assignee: "pi worker Fractious Jerakeen (wB:p7), steered by claude (session 14d06369)"
---

# Build the Brain Page Renderer

## Question

Implement Brain page rendering per the closed `decide-brain-svx-rendering-model`
resolution: routes `/<root>/<slug…>` over the registered roots
(`brain-roots.json` in dark-wizard is truth), MDSvX compilation with the
pi-notes standard set wiki-side AND per-root `.brain/components/**` compiled
for real (registration = consent; component failure is page-scoped and loud,
never build-fatal). Hard exclusions (captures/compaction-dumps/people/
private-family-health, `privacy: sensitive`) never render. Provenance
(root, path, commit) on every page. Wikilinks resolve per the namespace
policy; broken links render visibly. Prerendered static — no request-time
pipeline. Acceptance: the 2026-07-12 theo retro renders at its
brain.joelclaw.com URL from the dark-wizard root, replacing the static
stopgap page; `bun run check` and `bun run build` green.

## Resolution (2026-07-12)

Shipped and live. `src/routes/[root]/[...slug]` prerenders every registered
root from `brain-roots.json`; MDSvX with wiki-standard + root-local components
(page-scoped failure panels with folded source); hard exclusions verified
against the live site (`/user/people/...` 404s); provenance (repo, path,
commit) on every page; wikilinks local-first/global-unique with visible
failures. `bun run check` + `bun run build` green. The theo retro renders at
https://brain.joelclaw.com/user/resources/retros/three-things-theo-video-retro-2026-07-12/
(200, verified) — the static stopgap now redirects there. Spec note: the
registry names the dark-wizard checkout `user`, so URLs are `/user/...` per
the registry-controls-URLs rule. Unblocks retire-studio-viewer (parity call
is Joel's).
