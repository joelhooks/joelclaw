---
title: "Evaluate qmd as the Brain Knowledge-Recall Projection"
type: "step"
kind: "task"
status: "closed"
assignee: "pi worker (steered by claude session 14d06369)"
interaction: "AFK"
parent: "./observational-memory-brief.svx"
blockers: []
created_at: "2026-07-12"
closed_at: "2026-07-13"
---

# Evaluate qmd as the Brain Knowledge-Recall Projection

## Step

Joel flagged [tobi/qmd](https://github.com/tobi/qmd): local hybrid search
(SQLite FTS5 + sqlite-vec + RRF + local reranker, MCP server, TS SDK) over
markdown directories. Spike: does it become the KNOWLEDGE projection —
semantic recall over brain roots + observations — likely superseding
add-observation-embeddings (Typesense stays the STREAM projection: run
chunks, session scans)? Doctrine fit is exact: sqlite index in ~/.cache is
rebuildable, files stay truth, collections map to brain-roots.json.

Receipts wanted: install, collections over the three registered roots with
`**/*.svx` globs, `qmd query` receipts including the semantic query lexical
search misses ("what did we decide about memory compression" → the
node-cluster/reflector pages), latency numbers on flagg, MCP smoke test.

## Resolution (2026-07-13, pi worker spike)

ADOPT. qmd 2.5.3: 509 svx files across the three roots, 1,343 chunks
embedded in 36s on Metal; hybrid search ranked the observational-memory map
first for "what did we decide about memory compression" (semantic-only found
it with zero lexical hits — the exact receipt the embeddings ticket wanted).
MCP smoke passed. Verdict: qmd = KNOWLEDGE projection; Typesense = STREAM
projection. Full receipts: ../resources/qmd-brain-recall-spike.svx.
Caveats → productionization ticket: frontmatter/component chunk pollution,
explicit privacy caller-boundary, benchmark prompts outside indexed roots.
Supersedes add-observation-embeddings.
