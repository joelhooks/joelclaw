---
title: "Build the delivery-bar steering prose and morning digest"
type: "step"
kind: "task"
interaction: "AFK"
status: "closed"
closed_at: "2026-07-29"
assignee: "pi worker (flagg, herdr pane w28:pZ, 2026-07-29)"
parent: "./agent-comms-gateway-brief.svx"
blockers: []
created_at: "2026-07-29"
privacy: "private"
---

# Build the delivery-bar steering prose and morning digest

From [Decide the Telegram delivery bar](./decide-telegram-delivery-bar.svx):
the gateway delivers ~66 Telegram messages/day against an approved ~13. The
bar is settled; this step lands it in the gateway's steering prose and gives
the demoted traffic somewhere to go.

Resolved when:

- `roles/gateway.md` carries the bar: a ping means waiting-on-Joel-and-aging,
  important first-notice breakage, or an answer to what he asked. One incident
  = one thread until it closes. Worker DONE receipts never ping individually.
  Producer runbook cadence is evidence, not instruction. "Ack Before Working"
  is scoped to addressed inbound only.
- Digest guidance is written as intent, not spec (Joel: inference over
  specification): morning digest leads with "waiting on you" ranked by age ×
  importance, composed from live state at send time, context/thread/project
  aware by the agent's own judgment. One standalone ping per threshold
  crossing; digest carries it thereafter unless something material changes.
- A ~07:30 beat wakes the gateway to compose the digest — ride the existing
  pane-schedule/wake path; the digest is judgment, so it lands in the gateway
  queue, not a beat lane.

## Result (2026-07-29)

Delivered by pi worker (pane w28:pZ), reviewed and committed by steering.
`roles/gateway.md` (+62/−25): ack-first scoped to addressed inbound; ambient
gets `observe` and zero outbound; new "The Telegram Bar" section carries the
dropped-ball bar verbatim with one-incident-one-thread, one-ping-per-crossing,
worker-receipts-never-ping, and runbook-cadence-is-evidence; "Morning Digest"
section keeps composition as judgment (live-state re-check, waiting-on-you
first, handled-quietly below the fold) with mechanical successor-arm steps.
Digest beat: `morning-digest-schedule.ts` in the driver package — idempotent
07:30 America/Los_Angeles wake via the registry with readback verification and
cancel-on-unverifiable; armed by `pnpm --filter @joelclaw/agent-comms-driver
arm-morning-digest`. 52 driver tests pass.

Noted cut: the old "Steering Cadence" section (check-ins every 60–120s, the
frenzied-behavior stop rule) was removed wholesale — the cadence mandate was
itself chatter under the bar, but the frenzied-behavior stop was collateral;
restore it separately if missed.
