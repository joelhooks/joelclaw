---
title: "Worker task: build the transport slim-down"
type: "task"
created_at: "2026-07-21"
privacy: "private"
---

# Worker task: build the transport slim-down

You are a build worker for the **Agent Comms Gateway** effort. Execution is granted (Joel, cutover grilling, 2026-07-21).

1. Read the brief: `.brain/projects/agent-comms-gateway/agent-comms-gateway-brief.svx` — Vocabulary and standing calls bind you, especially **transport** (zero policy) and **fallback** (dead simple, heartbeat-key check, verbatim text, send-then-append).
2. Read your step: `.brain/projects/agent-comms-gateway/build-transport-slim-down.svx` and follow its scope exactly. Claim it: set `assignee: "pi (flagg, herdr worker pane, 2026-07-21)"`.
3. Your inputs: `asset-transport-policy-split.svx` (the keep/move file list), the fallback contract result (`design-fallback-contract.svx`), and the cutover plan §1–§3 (`plan-full-cutover.svx`).
4. **Deployment guard**: this work must be code-complete and tested but MUST NOT be deployed. Do not run `joelclaw gateway restart`, do not touch the running daemon. The cutover plan merges this during the supervised sitting; the live daemon runs from this checkout, so also do not leave `packages/gateway` in a state that breaks an unplanned restart — work must keep the daemon startable at every point (the priority-lane patch now in-tree is authorized to deploy; your changes are not, so gate them behind whatever seam your step specifies).
5. Also locate and verify in-tree the callback retry-storm staged source fix (cutover plan §5 disposition).
6. Surfaces: `packages/gateway/**` and its tests. Do not touch `packages/message-event-log` or the plugin tree — other workers own them.
7. Deliverables as file edits: the code; a `## Result (2026-07-21)` section in the step file with receipts (files changed, test output, the fallback path's two-conditional proof, retry-storm fix location); flip `status` to `closed` + `closed_at`; one ledger line in the brief.

Work autonomously, do not commit, print a DONE summary.
