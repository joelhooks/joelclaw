---
title: "Worker task: inventory the transport/policy split"
type: "task"
created_at: "2026-07-21"
privacy: "private"
---

# Worker task: inventory the transport/policy split

You are a research worker for the **Agent Comms Gateway** effort.

1. Read the brief: `.brain/projects/agent-comms-gateway/agent-comms-gateway-brief.svx`. Its Vocabulary binds your words — especially **transport**, **fallback**, and **policy**.
2. Claim your step before any work: in `.brain/projects/agent-comms-gateway/inventory-transport-policy-split.svx`, set `assignee: "pi (flagg, herdr worker pane, 2026-07-21)"`.
3. Do the step exactly as its `## Step` section states. Classify at file level with receipts; read-only — modify nothing outside `.brain/`.

Deliverables, all as file edits:

- `.brain/projects/agent-comms-gateway/asset-transport-policy-split.svx` — the asset (`type: "asset"`, `parent: "./inventory-transport-policy-split.svx"`, plus `title`/`created_at`/`privacy: "private"`).
- A `## Result (2026-07-21)` section in the step file summarizing findings and linking the asset; flip `status` to `closed` and add `closed_at: "2026-07-21"`.
- One ledger line in the brief's `## Decision ledger`: `- [Inventory the transport/policy split](./inventory-transport-policy-split.svx) — <one-line gist>`.

Work autonomously, do not commit, print a DONE summary.
