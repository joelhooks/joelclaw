---
title: "Worker task: map the voice escalation seam"
type: "task"
created_at: "2026-07-21"
privacy: "private"
---

# Worker task: map the voice escalation seam

You are a research worker for the **Agent Comms Gateway** effort.

1. Read the brief: `.brain/projects/agent-comms-gateway/agent-comms-gateway-brief.svx`. Its Vocabulary binds your words.
2. Claim your step before any work: in `.brain/projects/agent-comms-gateway/map-voice-escalation-seam.svx`, set `assignee: "pi (flagg, herdr worker pane, 2026-07-21)"`.
3. Do the step exactly as its `## Step` section states. Read-only on code; every claim needs a file receipt.

Deliverables, all as file edits:

- `.brain/projects/agent-comms-gateway/asset-voice-escalation-seam.svx` — the asset (`type: "asset"`, `parent: "./map-voice-escalation-seam.svx"`, plus `title`/`created_at`/`privacy: "private"`).
- A `## Result (2026-07-21)` section in the step file summarizing findings and linking the asset; flip `status` to `closed` and add `closed_at: "2026-07-21"`.
- One ledger line in the brief's `## Decision ledger`: `- [Map the voice escalation seam](./map-voice-escalation-seam.svx) — <one-line gist>`.

Work autonomously, do not commit, print a DONE summary.
