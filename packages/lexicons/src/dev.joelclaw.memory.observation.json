{
  "lexicon": 1,
  "id": "dev.joelclaw.memory.observation",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["content", "priority", "sessionId", "observedAt"],
        "properties": {
          "content": {"type": "string"},
          "priority": {"type": "integer", "minimum": 1, "maximum": 5},
          "sessionId": {"type": "string"},
          "observedAt": {"type": "string", "format": "datetime"},
          "category": {"type": "string"},
          "tags": {"type": "array", "items": {"type": "string"}}
        }
      }
    }
  }
}
