apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firecracker-images
  namespace: joelclaw
  labels:
    app: restate-worker
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
