# /etc/sudoers.d/agent-secrets
#
# Let the local operator recover the service-account-owned secrets daemon when
# its RPC loop is hung. Keep this command exact: sudoers wildcards match across
# argument boundaries and would grant broader launchd control.
joel ALL=(root) NOPASSWD: /bin/launchctl kickstart -k system/com.joel.agent-secrets
