# Copy to .env on each NAS. Prefer a verified tailnet interface address.
# Use 0.0.0.0 only when the NAS uses Tailscale userspace networking and has no tailnet interface address.
REPLICA_NAME=nas-a
REPLICA_BIND_ADDR=100.x.y.z
CONTAINER_NAME=joelclaw-critical-search-nas-a
