/** Retired compatibility pointer. Operational memory now uses composed flowing recall. */
export default function retiredMemoryRagExtension(): void {
  // Intentionally registers no tools, hooks, or workers.
}
